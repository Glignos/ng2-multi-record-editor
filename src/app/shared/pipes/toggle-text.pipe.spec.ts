import { ToggleTextPipe } from './toggle-text.pipe';

describe('ToggleTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleTextPipe();
    expect(pipe).toBeTruthy();
  });
});
